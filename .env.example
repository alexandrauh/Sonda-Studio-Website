# Required for Storyblok content fetches
VITE_STORYBLOK_TOKEN=replace-with-your-storyblok-token

# Optional override.
# Defaults to "draft" in development and "published" in production.
# VITE_STORYBLOK_VERSION=published
